[package]
name = "sparrow-client-contract"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
sparrow-core = { path = "../sparrow-core" }
