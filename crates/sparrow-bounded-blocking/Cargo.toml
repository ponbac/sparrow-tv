[package]
name = "sparrow-bounded-blocking"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
tokio = { version = "1", features = ["rt", "sync", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
